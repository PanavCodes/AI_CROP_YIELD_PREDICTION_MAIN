/**
 * User utilities for the crop prediction app
 * Placeholder implementation for now
 */

const getCurrentUser = () => {
  // Placeholder implementation
  // In a real app, this would extract user info from JWT token or session
  return {
    email: 'default@example.com',
    name: 'Default User'
  };
};

module.exports = {
  getCurrentUser
};